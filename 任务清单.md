# 任务清单

> 依据：`需求分析.md` | 项目目录：`C:\Users\24405\Pictures\Yolo_test`

## 工作规则

1. **未获明确要求，不得擅自执行任务**——只有用户明确指示时才动手。
2. **每完成一个任务，将其 `[]` 标记改为 `[X]`**，并在任务清单中同步更新。

## 任务（按依赖顺序）

```
[X]T1：确认并验证 yolo_env 环境（torch CPU、ultralytics、opencv 均已就绪）
[X]T2：确认采用 ASL Alphabet 数据集（29 类：A-Z + del/space/nothing，每类 3000 张）
[X]T3：数据已就绪于 C:\Users\24405\Pictures\Yolo_test\ASL\data\asl\asl（无需自采/标注）
[X]T4：分析 ASL 数据集结构与样本质量（图片尺寸/类别分布/异常样本）
[X]T5：编写数据准备脚本（按 8:2 划分 train/val、生成 data.yaml 或分类数据集格式）
[X]T6：编写 YOLOv8n CPU 训练脚本（epochs/imgsz/batch/device 配置）
[X]T7：执行训练并调优（全量 6.96 万张训练，验证集 top1_acc=100%，best.pt 已生成）
[X]T8：编写推理脚本（图片/视频文件识别，类别+置信度显示）——测试集 28/28 全对，单张 6ms
[X]T9：实现摄像头实时识别（OpenCV 捕获，q 键退出）
[X]T10：实现拼音/文字串累积逻辑（防重复、追加、清空、退格）——camera_text.py，并抽公共模块 hand_recognizer.py（高内聚低耦合重构）
[]T11：联调测试与性能优化（降分辨率/隔帧处理，保证 CPU 可用性）
[]T12：验收测试（对照需求文档 A1-A6）并编写演示流程说明
```

## 阶段二扩展：AI 拼音理解与补全（工具层 + Agent + RAG 架构）

### 背景与定位
手语打字机（T10）输出连续无空格字母串，无词边界、可能含识别错字。
本阶段借鉴拼音输入法"字母推算句子"原理，**按三层模块架构实现智能补全**：

- **工具层（Tool Layer）**：把拼音切分/词典/候选/RAG 检索封装成**标准工具接口**，向上层统一暴露 → 解耦、便于调用（对应方案文档 MCP"模块控制平面"的思想，但这里是系统内部能力层，非标准 MCP 协议）
- **Agent（智能决策调度）**：决定何时补全、低置信度/歧义时是否请求用户确认、是否调用 RAG → 处理歧义、优化流程
- **RAG（语义理解与知识增强）**：向量库存常用语料/历史句子，句意连贯、歧义消解、场景适配

整体分三层：`工具层 ← Agent 决策层 ← 界面层`，每层低耦合可替换。

### 任务（按依赖顺序）

```
[X]E1：学习调研拼音输入法"字母→拼音→句子"推算原理（拼音切分、n-gram、Viterbi、候选排序）—— E1_拼音输入法原理.md
[X]E2：设计三层模块数据流架构（字母流→工具层→Agent决策层→RAG增强→候选输出），画清工具层/Agent/RAG 各自接口与衔接 —— E2_架构设计.md
[X]E3：收集/构建拼音词典与词频语料（jieba 20万词 + pypinyin 生成 3 个 JSON 数据文件，含单字/整词频率表）
[X]E4：工具-词典：拼音→汉字候选映射（tool_dict.py：expand/expand_words/lookup_word，数据来自 E3 JSON，含大小写与空音节容错）
[X]E5：工具-切分：字母串→拼音音节切分（tool_seg.py：DFS 全切分+成词加权初筛，返回 Top-N；边界处理：英文串/空串/大小写）
[X]E6：工具-语言模型：unigram 打分（tool_lm.py + data/global_freq.json 全局字/词频；6/6 正确区分常见/罕见组合；整词词频偏低问题列入 E12）
[X]E7：工具-候选输出（tool_cand.py：整词匹配优先→切分拼凑兜底→LM打分排序→Top-N JSON；测试 nihao/xiexie/haoren 等均正确；jieba词频失真已通过高低分区解决；同音异义词留 E8/E12）
[X]E8a：Agent 决策引擎（agent.py：defer/output/confirm/ask_llm/fail 五决策 + llm_hook 挂载点；阈值参数化；测试全分支通过）
[X]E8b：大模型 API 接入（llm_client.py + config.py：DeepSeek 协议，容错解析+重试，5/5 成功，失败回落本地；与 Agent 集成测试通过）
[-]E9：RAG 语义增强【已搁置】——Milvus Lite 需 Python≥3.10，当前环境 py3.9 无法安装；后续如换 py3.10+ 环境或改用 ChromaDB 再启
[X]E10：界面层（camera_agent.py：pending字母累积→收手停顿触发Agent→候选栏数字键选择上屏；--demo 无摄像头验证通过；与 camera_text.py 并存低耦合）
[]E11：端到端联调：手语打 nihao → 工具切分 → Agent(本地候选→可选大模型) → 输出"你好"候选并可选
[]E12：性能与质量评估（候选延迟、准确率、识别错误联动调优；对比 本地 / 本地+大模型 / 本地+大模型+RAG 效果）
```

### 阶段二依赖关系

T10 → E1→E2→E3→E4→E5→E6→E7→E8→E9→E10→E11→E12
（E8 Agent 依赖 E4~E7 所有工具；E9 RAG 可后置但依赖 E7 候选格式）

### 阶段二备注

- 三层对应方案文档技术栈表思想：工具层≈MCP「解耦/工具化接口」（内部能力层，非标准 MCP 协议）；Agent=决策调度/处理歧义；RAG=歧义消解/句意连续/场景适配。
- E8 的 Agent 决策阈值可参考方案文档（conf<0.8 调增强、存在歧义请求用户确认）。
- **大模型 API（E8b）是语义补全的主增强路径**：词停顿触发一次（非字母级），输入带 top-K 概率，输出整句候选；失败/超时自动回落本地 n-gram 候选。
- RAG（E9）降级为可选增强：仅当"本地候选 + 大模型"都不满意时再触发向量检索，避免叠加延迟。
- 代码分层：tool_*.py（工具）→ agent.py（决策，含大模型 API 调用）→ rag.py（可选检索）→ ui 界面，保持高内聚低耦合。

## 依赖关系

T1→T2→T3→T4→T5→T6→T7→T8/T9→T10→T11→T12

（阶段二扩展 E1~E12 依赖 T10 完成后启动，见上）

## 辅助工具任务：字母混淆统计（confusion_camera.py）

> 依据：`需求分析_混淆统计.md` | 目的：统计"用户手型下易混淆字母对"，供大模型判断参考
> 说明：与主线和阶段二并行的小工具，不依赖 E11/E12；编号用 M 前缀避免与 T/E 冲突。

```
[X]M1：hand_recognizer.recognize() 扩展返回 top-k（topk 参数，默认 1，向下兼容不破坏 camera_*.py）
[X]M2：编写 confusion_camera.py 主循环（摄像头 + 意图字母切换 + top-5 显示）
[X]M3：实现统计逻辑（意图归一化、逐帧 top1 计数、moving/未检测到手过滤、low_conf 单列）
[X]M4：实现输出（data\confusion_*.json 落盘 + 控制台人读摘要/LLM 提示词）
[]M5：实测（摆 B/A 等手势验证统计正确性）并回归 camera_*.py 不受影响
```

## 依赖关系

T1→T2→T3→T4→T5→T6→T7→T8/T9→T10→T11→T12

（阶段二扩展 E1~E12 依赖 T10 完成后启动，见上）
（辅助工具 M1~M5 依赖 T10 的 hand_recognizer.py，独立于 E 系列，可随时开工）

## 备注

- 数据集自带标签（目录名即类别），已跳过 LabelImg 标注环节（原 T4~T7 已移除）。
- ASL 为美式手语，26 字母手型与中国手语有差异，用户已确认接受。
- 阶段二（E1~E12）：AI 拼音理解与补全，类输入法智能推测，见上方清单。
- 混淆统计工具（M1~M5）：详情见 需求分析_混淆统计.md。
