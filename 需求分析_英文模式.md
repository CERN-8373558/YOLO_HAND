# 英文模式（手语拼英文）—— 需求分析文档

> 基于《手语字母识别 + AI 拼音补全》项目扩展
> 版本：v1.0 | 日期：2026-09-05 | 状态：草案

---

## 1. 背景与动机

### 1.1 现状
现有系统（camera_agent.py）把手语字母串当作**汉语拼音**处理 → 本地切分/词典/LM 生成中文候选 + LLM 增强。
对**英文单词/句子**完全不支持：打 h-e-l-l-o 会被本地拼音引擎当成拼音处理（或 fail）。

### 1.2 目的
增加**英文模式**：手语比单词的字母 → 屏幕上累积字母串 → 发给大模型(LLM)优化成英文 → 上屏。
定位为中文版的**简化版**，主要差别：
- 不做本地猜词/词典/切分（英文模式跳过本地候选）
- 依赖 LLM 做纠错 + 组词 + 润色，目标输出英文

### 1.3 决策记录（需求澄清结论）
| 问题 | 结论 |
|------|------|
| 英文版含义 | 识别英文手指字母 → LLM 拼成英文单词/句子 |
| 与现版关系 | **同一套程序加英文模式**（camera_agent.py 内切换） |
| 核心流程 | 手语字母→累积字母串→发 LLM→LLM 返回优化英文 |
| 本地猜词 | 英文模式**跳过**本地候选生成，直接走 LLM |
| 是否纠错 | 靠 LLM 纠错（字母可能识别错），无本地混淆纠错 |
| 候选上限 | 同现版，最多 5 个 |
| 大小写 | 全部小写（后续润色再处理） |
| LLM 结果用途 | 单字母串替换 + 整句润色**两者都要**（类似中文 S/T 键） |
| 模式切换 | **运行时按键**切换中/英 |
| 手语模型 | 复用 asl_demo_dark-v2（ASL 26 字母直接对应英文字母） |
| 词边界 | 英文模式仅纯字母累积 + LLM 优化；词间空格靠用户/LLM 处理 |
| 验收 | 不设本地验收门槛，LLM 返回英文即视为成功（提示词工程层改动） |

---

## 2. 环境约束
- 同现有：yolo_env311、camera_agent.py、hand_recognizer.py、asl_demo_dark_v2-best.pt
- LLM：DeepSeek（config.py 已配 key）

---

## 3. 功能需求

### 3.1 核心功能
| 编号 | 功能 | 说明 | 优先级 |
|------|------|------|--------|
| EN1 | 模式切换 | camera_agent 运行中按键在 中文/英文 间切换 | 高 |
| EN2 | 英文模式识别累积 | 字母进 pending 同现版，不触发本地拼音候选 | 高 |
| EN3 | LLM 英文补全 | 收手停顿 → 字母串发 LLM → 返回英文候选/词/句 | 高 |
| EN4 | 英文整句润色 | 已上屏英文文本发 LLM 润色（对应英文版 T 键） | 中 |
| EN5 | 界面状态提示 | 显示当前模式(中/英)，方便切换时不迷路 | 中 |

### 3.2 交互流程
```
启动 camera_agent.py（默认中文模式，照旧）
  ↓ 按键切模式（如 n 键或 m 旁边新键）→ 屏幕状态显示 "EN"
  ↓
手语比 h-e-l-l-o → 字母累积到 pending，屏幕显示字母串 hello(未确认上屏)
  ↓ 收手停顿 0.6s
不发本地候选，直接把字母串发 LLM
  ↓ LLM 返回
英文候选行(如 hello / hellow) → 数字键选 → 上屏
  ↓ 也可
按整句润色键 → 已上屏整段英文发 LLM 润色成通顺句子
```

### 3.3 与中文模式的区别（关键）
| 环节 | 中文模式（现版） | 英文模式（新增） |
|------|-----------------|-----------------|
| 字母累积 | pending | 同（共用） |
| 收手停顿触发 | Agent 本地决策(tool_cand) | **跳过本地，直接发 LLM** |
| 候选来源 | 本地候选 + LLM 行 | 仅 LLM |
| 本地候选栏 | 显示 | 不显示/清空 |
| 补全 prompt | 中文拼音推断 | 英文拼词纠错 |
| 润色 prompt | 中文润色 | 英文润色 |
| 上屏 | 中文字符 | 英文字母/单词 |

### 3.4 LLM 提示词设计（改动核心）
现有 `llm_client._build_prompt` 是中文专用。英文模式需：
- 新 prompt：把字母串当作**英文单词的字母序列**（可能有识别错），推断最可能的英文单词
- 示例引导：`输入 "hllo" → hello`、`输入 "wrold" → world`
- system 角色：English sign-language letter helper，输出英文
- 整句润色：English proofreading，输出通顺英文句
- 复用 `_parse_response` 解析（JSON 格式兼容），仅换 prompt 与 system

### 3.5 模式切换键设计
- 建议：`n` = 切中/英（或 Esc 已被忽略LLM用，需避免冲突）。候选：
  - 用 `n`（English 首字母，易记）或 `e`；注意 camera_agent 现有键：q/m/c/s/t/数字/Esc
  - `e` 未被占用 → 用 **e 键切 EN/CN**，屏幕状态条显示 "EN"/"CN"

---

## 4. 非功能需求
| 编号 | 类别 | 需求 |
|------|------|------|
| N1 | 兼容 | 中文模式一切行为不变（回归） |
| N2 | 编码 | 输出英文 ASCII，无编码问题 |
| N3 | 失败回落 | LLM 失败 → 字母串原样保留/提示，不阻塞 |

---

## 5. 验收标准
1. 中文模式下现有功能回归正常（打拼音仍出中文）
2. 切 EN 后比 h-e-l-l-o → LLM 返回英文 hello 候选 → 数字键上屏
3. 英文整句润色键把乱序英文润色成通顺句
4. 界面显示当前模式，切换不丢已累积内容
