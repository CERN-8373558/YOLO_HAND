# 手语字母智能识别系统 —— 需求分析文档

> 基于《手语智能识别方案设计：基于 RAG + MCP + YOLO + Agent 的小而美工程实践》方案
> 版本：v1.0 | 日期：2026-09-01 | 状态：草案

---

## 1. 项目概述

### 1.1 项目背景
手语是听障人士的主要交流方式。本文档规划一套基于 YOLO 的中国手语字母（拼音字母表）识别系统，参考"小而美"模块化方案，先实现静态手势识别，为后续连续手语识别打基础。

### 1.2 项目定位
- **阶段一（本期）**：静态中国手语字母识别，输出拼音/文字串
- **阶段二（远期）**：连续手语识别（增加时序模型 + Agent/RAG 消歧）

### 1.3 参考架构
参考文中四层架构思想，本期仅实现核心层，预留扩展接口：

| 层级 | 本期实现 | 远期预留 |
|------|---------|---------|
| YOLO（视觉感知） | ✅ 手部/手势检测 | 关键点提取（21点） |
| 原子层（最小单位） | 单帧手势分类 | 手型+动作+位置+时序 JSON 原子 |
| MCP（模块控制） | 简单脚本/CLI | RESTful API 化 |
| Agent / RAG | ❌ 不实现 | 歧义消解、句意连续 |

---

## 2. 用户与场景分析

### 2.1 目标用户
- 个人学习者 / 开发者
- 演示对象：老师、同学、答辩评委

### 2.2 使用场景
| 场景 | 说明 |
|------|------|
| 开发/演示 | 本地 PC 上运行，展示手语字母识别过程 |
| 数据验证 | 验证标注数据的正确性和模型效果 |
| 学习教学 | 了解 YOLO 训练与推理流程 |

### 2.3 环境约束
- **硬件**：无独立 GPU，纯 CPU（Intel/AMD 处理器）
- **系统**：Windows 10/11
- **软件**：Python 3.9（conda 环境 `yolo_env`）

---

## 3. 功能需求

### 3.1 核心功能

| 编号 | 功能 | 说明 | 优先级 |
|------|------|------|--------|
| F1 | 摄像头实时识别 | 打开电脑摄像头，逐帧识别手语字母并显示 | 高 |
| F2 | 图片/视频文件识别 | 读取本地图片或视频文件进行识别 | 高 |
| F3 | 拼音文字串累积 | 识别出的字母拼成拼音/文字串，可清空 | 高 |
| F4 | 识别结果显示 | 在 OpenCV 窗口显示检测框、字母标签、置信度 | 高 |
| F5 | 结果输出 | 支持保存识别结果（文字/视频） | 中 |

### 3.2 功能细节

**F1 摄像头实时识别**
- 实时显示摄像头画面，叠加检测框
- 手势保持时重复识别同一字母，变化后追加到文字串
- 每帧处理延迟 ≤ 200ms（CPU 可接受范围）

**F3 文字串累积规则**
- 识别到新字母且与前一个不同 → 追加
- 同一字母连续帧 → 只记一次（防重复）
- 提供清空、退格操作

**F4 显示内容**
- 检测框 + 类别名（如 `A`、`B`）
- 置信度百分比（如 `0.92`）
- 画面角落显示累计文字串

### 3.3 非功能需求

| 编号 | 类别 | 需求 |
|------|------|------|
| N1 | 性能 | CPU 上单帧推理 ≤ 200ms |
| N2 | 易用性 | 命令行启动，一条命令运行；窗口按 `q` 退出 |
| N3 | 可维护性 | 代码模块化（数据/训练/推理/UI 分离），注释清晰 |
| N4 | 可扩展性 | 类别配置集中管理，新增字母无需改代码 |
| N5 | 鲁棒性 | 支持光照变化、不同肤色、单手/双手场景 |

---

## 4. 数据需求

### 4.1 数据现状
- 尚未开始收集数据，需从零准备。

### 4.2 数据规模建议（CPU + 起步阶段）

| 类别 | 每类图片数 | 说明 |
|------|-----------|------|
| 训练集 | 60~100 张 | 不同角度、光照、背景 |
| 验证集 | 20~30 张 | 训练时评估 |
| 测试集 | 20~30 张 | 最终验收 |

- 中国手语字母约 26 类（含拼音字母），总量约 3000~4000 张
- 每类至少 100 张不同图片，避免模型过拟合

### 4.3 数据收集要求
- 使用手机/相机拍摄，同一手型多角度多距离
- 背景尽量多样（桌面、墙面、窗外），提高泛化
- 单人拍摄为主，可引入多人手部样本增强

### 4.4 标注要求
- 工具：LabelImg（PASCAL VOC 格式，输出 XML）
- 类别名：小写字母，如 `a`、`b`、`c`...
- 框要紧贴手部，不包含手腕以下过多区域
- 标注完成后转换为 YOLO 格式（txt），按 `train` / `val` 划分

### 4.5 目录结构约定

```
mydataset/
├── data.yaml              # 类别和路径配置
├── images/
│   ├── train/
│   └── val/
├── labels/
│   ├── train/
│   └── val/
```

---

## 5. 技术方案需求

### 5.1 技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| 检测模型 | YOLOv8n（nano） | 轻量，CPU 可跑，文中推荐 |
| 深度学习框架 | PyTorch 2.8 + CUDA=CPU | 已安装于 yolo_env |
| YOLO 库 | ultralytics 8.4+ | 已安装，支持训练/导出/推理 |
| 图像处理 | OpenCV | 摄像头/文件读写、显示 |
| 训练 | ultralytics `train()` | 现成接口，配置简洁 |

### 5.2 训练配置建议（CPU）

| 参数 | 建议值 | 说明 |
|------|--------|------|
| model | yolov8n.pt | 预训练权重起步 |
| epochs | 50~100 | CPU 慢，可先 50 验证 |
| imgsz | 640 | 默认尺寸 |
| batch | 8~16 | CPU 内存允许范围内 |
| device | cpu | 无 GPU |
| patience | 20 | 早停，防过拟合 |

### 5.3 推理流程

```
摄像头/文件 → YOLO 推理 → 检测框+类别+置信度
    → 累积规则 → 拼音文字串 → OpenCV 窗口显示
```

### 5.4 输出要求
- 命令行可运行：`python yolo_demo.py`（摄像头）/ `python yolo_demo.py 图片路径`
- 显示窗口实时刷新，`q` 退出

---

## 6. 验收标准

| 编号 | 验收项 | 标准 |
|------|--------|------|
| A1 | 数据集完成 | 每类 ≥100 张标注图，train/val 划分正确 |
| A2 | 训练完成 | 模型收敛，val mAP50 ≥ 0.85 |
| A3 | 实时识别 | 摄像头下常见字母识别准确，帧率可用（≥5 FPS CPU） |
| A4 | 文字累积 | 手势变化正确累积拼音串，可清空 |
| A5 | 文件识别 | 图片/视频文件可正常识别 |
| A6 | 演示流畅 | 答辩/演示时能完整跑通流程 |

---

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| CPU 训练慢 | 周期超时 | 用 YOLOv8n、imgsz 降到 480、epoch 先 30 |
| 数据量不足 | 准确率低 | 数据增强（旋转/翻转/亮度），后续补拍 |
| 摄像头兼容问题 | 无法实时 | 测试不同的 capture 索引，提供参数 |
| 单帧延迟高 | 卡顿 | 降分辨率推理 + 隔帧处理 |

---

## 8. 里程碑计划（1-2 周）

| 阶段 | 内容 | 耗时 |
|------|------|------|
| M1 环境与数据 | 确认环境、收集/拍摄图片 | 2~3 天 |
| M2 标注 | LabelImg 标注并转换格式 | 2~3 天 |
| M3 训练 | YOLOv8n 训练与调优 | 1~2 天 |
| M4 推理 Demo | 摄像头/文件识别 + 文字累积 | 1~2 天 |
| M5 验证与文档 | 验收测试、整理演示流程 | 1 天 |

---

## 9. 远期扩展（阶段二预留）

- 手部关键点检测（yolov8n-pose）→ 时序模型（LSTM/Transformer）→ 孤立词/连续手语
- MCP 服务化：`/detect_sign_atoms` 等 API
- Agent 决策 + RAG 歧义消解
- Web 界面

---

## 附录

### 依赖清单（当前环境已装）
```
torch==2.8.0+cpu
torchvision==0.23.0+cpu
ultralytics==8.4.137
opencv-python
numpy
```
