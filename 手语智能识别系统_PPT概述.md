# 手语智能识别系统 · 项目概述

## 一、项目名称
**"指尖传声"—— 基于 YOLOv8 的手语字母识别与 AI 智能拼音补全系统**

## 二、选题理由（Why）
- **社会价值**：全球约 4.3 亿听障人士，约 7000 万人以手语为日常交流方式，手语识别是 AI 无障碍的重要落地方向。
- **技术取舍**：端到端大模型手语识别"部署复杂、门槛高、难扩展"；本项目借鉴《RAG+MCP+YOLO+Agent 小而美工程实践》思路，走"轻量模块化 + 可落地"路线。
- **学习价值**：完整走通"数据 → 训练 → 推理 → 交互 → 智能增强"全流程，兼顾 CV 与传统 NLP/大模型，工程实践性强。
- **约束驱动**：纯 CPU（无独立 GPU）环境仍能实现毫秒级推理与流畅演示，证明轻量方案的可行性。

## 三、项目简介（What）
一套"**手语打字机 + 拼音输入法**"式的完整系统，采用 **工具层 ← Agent 决策层 ← 界面层** 三层低耦合架构：
- **阶段一 · 手语打字机**：摄像头/图片/视频中识别 ASL 手语字母（29 类：A–Z + del/space/nothing），识别结果自动累积成文字串；支持防抖确认、退格、清空等"用眼睛打字"交互。
- **阶段二 · AI 拼音补全**：借鉴拼音输入法原理，把手语拼出的连续字母串经"拼音切分→词典映射→语言模型打分→候选排序"翻译成中文词句，再接入 **Agent 决策引擎 + 大模型（DeepSeek）语义增强**，实现"打 nihao → 出『你好』"的智能补全与歧义消解。
- **RAG 语义增强（已纳入架构）**：作为决策链路的**可选增强层**常驻系统——仅当"本地候选 + 大模型"都不满意时触发向量检索（常用语料 / 用户历史 / 场景语料），做候选重排、纠错与个性化补全，避免叠加延迟。

## 四、技术亮点 / 创新点
- **双阶段识别**：MediaPipe 手部关键点(21点) 定位裁剪 + YOLOv8n 分类，先"找手"再"认字"，专注轻量。
- **视觉→控制**：把 space/del 手势映射为键盘操作，形成人机交互闭环。
- **防抖状态机**：同一手势连续 8 帧确认 + 运动检测，解决手势过渡期误识别。
- **Agent 分层决策**：`defer / output / confirm / ask_llm / fail` 五态决策，阈值参数化，本地不可靠时自动请求大模型、失败可回滚，稳定不崩。
- **RAG 按需增强**：三级增强路径（本地 n-gram → 大模型 → RAG 向量检索），**分层降级、按需触发**，兼顾语义质量与实时性；向量库接常用语料、用户历史与场景词库，支撑句意连贯与歧义消解。
- **高内聚低耦合**：识别核心抽为 `hand_recognizer.py` 公共模块，多入口复用；工具层/Agent/界面三层单向依赖，可独立测试与替换。

## 五、技术栈（Stack）
| 层面 | 技术 |
|------|------|
| 深度学习框架 | PyTorch 2.8（CPU）+ ultralytics 8.4 |
| 视觉模型 | YOLOv8n-cls（分类）、MediaPipe HandLandmarker |
| 图像处理 | OpenCV（实时捕获/显示） |
| 数据工程 | ASL Alphabet 数据集 8.7 万张 + jieba/pypinyin 词库语料构建 |
| NLP/决策 | 全切分 + Viterbi、n-gram 语言模型、Agent 决策引擎、DeepSeek LLM API |
| RAG 检索 | 向量库（Chroma/Faiss）+ 常用语料/用户历史/场景语料 |
| 环境 | Windows + Python 3.9（conda yolo_env） |

## 六、项目成果 / 数据
- 全量训练：约 8.7 万张（29 类），**验证集 top-1 准确率 100%**
- 测试集 28/28 全对，**CPU 单张推理约 6ms**，摄像头实时可用
- 图片/视频/摄像头三种输入 + 打字机 + 智能补全（含 RAG 增强链路）架构已打通

## 七、未来计划（Next）
1. **短期收尾**：T11/T12 性能调优与验收测试（对照需求 A1–A6 验收标准），整理演示流程。
2. **阶段二收官**：E11 端到端联调、E12 候选延迟与准确率评估（本地 / 本地+大模型 / +RAG 三档对比）。
3. **迈向连续手语识别**：引入 CE-CSL 中文连续手语视频数据集，YOLO 关键点 → 时序模型（LSTM/Transformer）→ 词语/句子级识别。
4. **服务化与硬件升级**：核心能力封装为 MCP 式统一 API/Web 界面；移植到 RTX 3060 GPU，训练提速约 20–50 倍，支持手机端轻量化。

## 八、结语
用"小而美"的方式，让 AI 手语识别**在普通电脑上就能跑起来**——以打字机式交互解决沟通刚需，以输入法式智能补全（本地 + 大模型 + RAG 分级增强）打通从"手势"到"句子"的最后一公里，既是技术实践，也是面向无障碍的温暖尝试。
