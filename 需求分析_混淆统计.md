# 字母混淆统计工具 —— 需求分析文档

> 基于《手语字母识别 + AI 拼音补全》项目扩展
> 版本：v1.0 | 日期：2026-09-03 | 状态：草案

---

## 1. 背景与动机

### 1.1 问题
手语字母的手势与其对应的英文字母**形态上无任何关联**（如 A 的手型与字母"A"无关），
因此识别错误的模式是**数据驱动**的、不遵循常规的"形近字母"规律（如 O/Q、M/N 之类）。
系统无法预先假设"哪些字母容易互相混淆"。

### 1.2 目的
做一个工具，在实际手语（用户自己的手型）下**统计出现模型容易把哪些字母互相认错**，
产出结构化"易混淆说明"，供后续**发给大模型判断**（Agent 补全/纠错时参考）。

### 1.3 决策记录（需求澄清结论）
| 问题 | 结论 |
|------|------|
| 混淆数据从哪来 | 现有模型(asl_demo-6-best.pt) + 摄像头实际识别（不重训、不采图） |
| 为什么不用验证集混淆矩阵 | 用户手型与标准 ASL 数据集有差异，需统计"自己的手型" |
| 输出形态 | 易混淆对列表（归一化混淆率） |
| 类别范围 | 仅 26 字母 A-Z（排除 del/space/nothing） |
| 样本怎么记 | **逐帧都记**（不做稳定帧合并） |
| 每帧记什么 | 识别 **top-5 完整分布**（含概率） |
| top-k 的 k | top-5 |
| 意图字母怎么告知 | 键盘字母键切换当前意图字母 |
| 每个字母采多久 | 手动按"停止/下一类"控制，时间不限 |
| 没检测到手 | 跳过不记 |
| 手在移动(过渡) | 跳过不记（防误判噪声） |
| 输出落盘 | `yolo_demo\data\confusion_*.json` + 控制台可读打印 |
| 给 LLM 的形态 | JSON（结构化）+ 人读提示词，两者都要 |

---

## 2. 环境约束

- 运行环境：`yolo_env311`（Python 3.11.16，yolo_demo 目录下运行）
- 依赖现成模块：`hand_recognizer.py`（定位手 + 裁剪 + 分类）
- 模型：`models\asl_demo-6-best.pt`（29 类分类）
- 手部模型：`hand_landmarker.task`

---

## 3. 功能需求

### 3.1 核心功能

| 编号 | 功能 | 说明 | 优先级 |
|------|------|------|--------|
| C1 | 摄像头实时识别 | 逐帧识别字母 top-5 | 高 |
| C2 | 意图字母切换 | 键盘选当前"想测的字母" | 高 |
| C3 | 混淆统计 | 按意图归一化统计 top1 错分率 | 高 |
| C4 | 结果输出 | JSON 落盘 + 控制台人读摘要 | 高 |
| C5 | hand_recognizer 扩展 | 增加返回 top-k 的能力 | 高 |

### 3.2 交互流程

```
启动: python confusion_camera.py [--model ...] [--threshold 0.05]

画面实时显示:
  - 摄像头画面 + 检测框 + 当前帧 top-5（字母+百分比）
  - 当前"意图字母"高亮（如 [当前: B]）
  - 累计统计（B 已采 N 帧，其中认成 C x 帧）

操作:
  - 按 字母键 a-z  → 切换意图字母（同时把上一个意图封存/保留累计）
  - 按 空格/回车    → 手动停止当前意图，切下一个（等同下一类）
  - 按 q            → 结束并输出统计
  - 其余按 camera 系列惯例
```

### 3.3 统计规则

**类别**：仅 26 字母 A-Z；若模型 top1 落入 del/space/nothing → 计为"其他"(other)，
不参与字母混淆归一化（单独统计占比）。

**过滤**：
- 手未检测到 → 跳过
- `moving == True`（手在移动）→ 跳过
- 意图字母尚未设置（未按任何字母键）→ 可显示但不统计

**归一化**（判定标准）：
- 对每个意图字母 X，统计其**有效帧总数** N_X（被识别到手且意图=X 的帧）
- X → Y 的混淆率 = （意图 X 的帧中 top1=Y 的帧数）/ N_X
- 同字母命中（top1 = X）= 正确率；top1 ≠ X = 错分，落进其他意图字母
- 低置信帧（top1 conf < 阈值，默认 0.5）单列为 `low_conf` 占比，不进具体字母混淆

**top-k 分布**：每帧记录 top5 `[{letter, prob}]`，供 JSON 存原始聚合；易混淆对计算只用 top1。

### 3.4 易混淆对判定（阈值）

- 输出：对每个意图 X，列出 top1 错分成 Y 且混淆率 > threshold(默认 0.05=5%) 的字母
- 例：`B 常被认成: C(12%) D(5%)`；`A 常被认成: H(9%)`
- 若 X 正确率 > 90% 且无超阈值错分 → 标注"稳定"（可选入 JSON 但不进提示词）

### 3.5 输出

**JSON**（`data\confusion_<时间戳>.json`）：
```json
{
  "meta": {"model": "...", "threshold": 0.05, "date": "...", "samples": {...}},
  "confusion": {
    "B": {"total": 800, "correct": 0.88, "low_conf": 0.03,
          "targets": {"C": 0.12, "D": 0.05, ...}},
    ...
  },
  "top5_dist": { "B": [{"letter": "C", "avg_top2_rate": 0.2}, ...] }
}
```

**控制台人读摘要**（供直接复制喂 LLM）：
```
=== 字母混淆统计(易混淆对，阈值>5%) ===
A → H 9% | 稳定
B → C 12% D 5%
C → A 8%
...
低置信帧较多: D(15%) F(12%)   ← 提示这些字母模型拿不准
```

### 3.6 hand_recognizer 扩展（前置改动）

`recognize(frame, topk=1)` 增加返回 top-k：
- 现有返回 dict 加键 `topk: [{letter, prob}, ...]`（降序，默认1个，向下兼容）
- 注意：现有 `predict` 已取 `results[0].probs.data`，扩展只需排序取前 k，代价极小
- 需回归：camera.py / camera_text.py / camera_agent.py 不受影响（不传 topk 默认仍取 top1）

---

## 4. 非功能需求

| 编号 | 类别 | 需求 |
|------|------|------|
| N1 | 性能 | 单帧处理 ≤ 200ms（CPU），统计不拖慢识别 |
| N2 | 兼容 | 不改动现有 camera_*.py 行为 |
| N3 | 可读 | 输出中文编码 UTF-8，PowerShell 下可用 PYTHONIOENCODING=utf-8 |
| N4 | 可配置 | 阈值/模型/手部模型路径可命令行覆盖 |

---

## 5. 验收标准

1. 运行后按 b 键，摆 B 手势 100+ 帧，统计的意图=B 有效帧数与实际大致相符
2. 正确摆手时 top1 大概率=意图字母；有意摆近似手势可观察到错分落向某几个字母
3. 退出生成 data\confusion_*.json，内容含 confusion + top5_dist，格式如上
4. 控制台输出"B 常被认成: ..."式摘要，UTF-8 无乱码
5. camera.py / camera_text.py / camera_agent.py 仍正常运行（回归通过）
